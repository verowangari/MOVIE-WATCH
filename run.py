from app import app

if __name__ == '__main__':
    app.run(debug = True) #Here we import the app instance. We then check if the script is run directly and then use the app.run() method to launch our server. 